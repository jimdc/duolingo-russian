# Screenshots

Drop PNGs here and reference them from the top-level `README.md`:

- `gender-stress.png` — a lesson showing gender colours + stress marks
- `verb-tense.png` — a lesson showing verbs coloured by tense

Goal: auto-generate these from a visual test that renders the captured
fixtures (`tests/fixtures/`) with the bundled userscript and screenshots them,
so the README images stay current with each release.
